-- // antitool by TastyVanillaa
-- // my methods are epic.
local players = game:GetService("Players")
local player = players.LocalPlayer
local character = player.Character
local arm = character["Right Arm"]
local arm2 = character["Left Arm"]
local torso = character.Torso
torso["Right Shoulder"]:Destroy()
while task.wait() do
arm.CFrame = arm2.CFrame * CFrame.new(3, 0, 0)
arm.AssemblyLinearVelocity = Vector3.zero
arm.AssemblyAngularVelocity = Vector3.zero
end
